<?xml version="1.0" encoding="utf-8"?>
    <menu xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        app:showAsAction="always"
        android:icon="@drawable/heart1"
        android:enabled="true"
        android:checked="true"
        android:title="TODO"></item>
    <item android:id="@+id/menuBtn"
        app:showAsAction="always"
        android:icon="@drawable/write"
        android:iconTint="@drawable/item_color"
        android:enabled="true"
        android:checked="true"
        android:title="community" />


</menu>
    <!--하단에 텍스트도 넣고싶으면 labelVisibilityMode (labeled)-->
    <!--title is label that show text-->
    <!--tooltipText is tooltip that remain clicking...-->
